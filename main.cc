//Justin Giguere
//testing routing table

#include "RoutingTable.h"
#include "KBucket.h"
#include <iostream>

using namespace std;

//Pre: N/A
//Post: Heap stuff is deleted
int main() {
  /*   temporaryily commented out
  int address = 0; //We don't care about address for this testing
  bool end = false;
  int id;
  RoutingTable myTable; 
  while (!end) {
    cout << "input a node id (aka a numbe) that hasn't been seen yet" << endl;
    if (id == -1) {
      end = true;
    }
    else {
      cin >> id;
      bool added = addNode(id, address);
      cout << "myTable" << endl;
      myTable.print(table);
    }
    
    } */
  return (0);
}
